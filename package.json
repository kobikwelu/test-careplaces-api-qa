{
  "name": "test-api-careplaces-qa",
  "version": "0.0.1",
  "dependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.2.1",
    "nodemon": "^2.0.6",
    "supertest": "^6.0.1"
  },
  "engines": {
    "node": "15.x.x",
    "npm": "7.x.x"
  },
  "scripts": {
    "test": "./node_modules/.bin/mocha --recursive --exit",
    "smoketest": "mocha tests --grep SMOKE",
    "regressiontest": "mocha tests --grep REGRESSION"
  }
}
